//make basic setup
import React from 'react';
import { getLeaderboard } from '../services/api';

const Leaderboard = () => {
    return (
        <div>
            
        </div>
    );
};

export default Leaderboard;